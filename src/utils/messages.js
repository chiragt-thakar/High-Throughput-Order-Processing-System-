const User_email_is_required =
    'User Email is required for registration.';

const User_name_is_required =
    'User Name is required for registration.';

const User_password_is_required =
    'User Password is required for registration.';

const Email_is_already_registered = "Email is already registered"

const Order_fetched_successfully = 'Orders fetched successfully';
const Order_created_successfully = 'Order created successfully';

const Admin_statistics_fetched_successfully = 'Admin statistics fetched successfully'
const Invalid_credentials = 'Invalid credentials'

module.exports = {
    User_email_is_required,
    User_name_is_required,
    User_password_is_required,
    Order_fetched_successfully,
    Order_created_successfully,
    Admin_statistics_fetched_successfully,
    Email_is_already_registered,
    Invalid_credentials
};
